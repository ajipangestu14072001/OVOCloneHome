<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.OVO" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.OVO" parent="Base.Theme.OVO" />

    <style name="SplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/mainColor</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/jetpack_compose_logo</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.OVO</item>
    </style>

    <style name="ShapeAppearanceOverlay.App.CornerCircle" parent="">
        <item name="cornerSize">50%</item>
    </style>

    <style name="ChipTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@android:color/transparent</item>
        <item name="tabBackground">@drawable/selector_tab</item>
        <item name="tabSelectedTextColor">?android:textColorPrimary</item>
    </style>

</resources>